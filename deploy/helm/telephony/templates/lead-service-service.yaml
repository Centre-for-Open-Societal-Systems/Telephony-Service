apiVersion: v1
kind: Service
metadata:
  name: lead-service-service
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.leadService.serviceType }}
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: lead-service
